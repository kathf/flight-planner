<div class="container-fluid content">
  <div class="row map-row">

    <div class="col-sm-3">
      <div class="form-horizontal">

        <%= form_for @way, remote: true do |f| %>
          <!-- <%= f.fields_for :origin do |o| %>
            <%= o.collection_select :origin_id, @airports, :id, :name, {}, {class: "form-control"} %>
          <% end %> -->
          <%= f.collection_select :origin_id, @airports, :id, :name, {}, {class: "form-control"} %>
          <%= f.collection_select :destination_id, @airports, :id, :name, {}, {class: "form-control"} %>
          <%= submit_tag "Find your way", class: "btn btn-default" %>
        <% end %>

      </div>
    </div>

    <div class="col-sm-9 full-height">
      <div id="map-canvas"></div>
    </div>

  </div> <!-- row -->
  <div class="row">

  </div>

</div> <!-- container -->
<div class="black full-height"></div>
